import React from 'react';

const Register = () => {
    return (
        <div>
            {/*    <form onSubmit={handleregister}>
    <input onBlur={HandleEmailOnblur} type="email" name="email" id="" placeholder='your Email' /> 
    <br />
    <input onBlue={HandlePasswordBlue} type="password" name="password" placeholder='Enter Your Password' />
    <br />
    <button type="submit">Register</button>
    </form> */}

            
        </div>
    );
};

export default Register;